#include <stdio.h>
#include <string.h>
#include "compression.h"

/* Take encoded data and remove leading zeroes */
int compress(char *c_data, char *e, int k, int block_size)
{
	int write_size = 0;
	
	return write_size;
}

void decompress(char *d_data, char *c_data, int size)
{

}
