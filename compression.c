#include <stdio.h>
#include <string.h>
#include "compression.h"

void compress(int *input_data, int k, int block_size, char *filename)
{
	char byte_buf = 0;

	write_byte(byte_buf,"Output/compressed.out");
}

